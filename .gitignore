# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# If the skill is ever exercised in-place, these are the outputs it produces.
# Don't commit them — they're per-run artifacts.
audit/
tests/
.playwright-mcp/
node_modules/
dist/

# Secrets — defense in depth even though the skill itself doesn't need any
.env
.env.*
!.env.example
*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
